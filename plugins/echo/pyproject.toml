[project]
name = "nahida-plugin-echo"
version = "0.1.0"
description = "A simple echo plugin for nahida-bot — /echo and /ping"
requires-python = ">=3.12"
dependencies = [
    "nahida-bot-sdk>=0.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

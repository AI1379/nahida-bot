[project]
name = "nahida-bot-sdk"
version = "0.1.0"
description = "Public SDK types for nahida-bot plugin development"
readme = "README.md"
license = { text = "WTFPL" }
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0.0",
    "pyyaml>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
